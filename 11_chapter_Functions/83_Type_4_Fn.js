function sumOfNumbers(a, b) {
    return a + b;
}
let result = sumOfNumbers(5, 10);
console.log(result); // 15, because the function returns the sum of a and b
console.log(sumOfNumbers(20, 30)); // 50, because the function returns the sum of a and b